redis
opencv-python-headless
vosk
numpy
sentence-transformers
requests
fastapi
uvicorn
yt-dlp

# Image processing
Pillow>=9.0.0

# ONNX Runtime - Fast CPU inference
onnxruntime>=1.16.0

# MediaPipe - Google's ML solutions (Apache 2.0)
mediapipe>=0.10.0

# Detection Models (Apache 2.0 licensed) - fallback if no ONNX
transformers            # For RT-DETR and Moondream2 VLM
torch                   # PyTorch (only if not using ONNX)
torchvision

# VLM Dependencies (Moondream2)
einops>=0.6.0           # Required by Moondream2 architecture
accelerate>=0.25.0      # Required for device_map in transformers

supervision>=0.16.0      # ByteTrack tracking and utilities

# Optional: Ultralytics (AGPL-3.0 - DISABLED by default)
# Set ENABLE_ULTRALYTICS=true to use
# ultralytics  # Commented out - only install if testing required
