redis
opencv-python-headless
vosk
numpy
sentence-transformers
requests
fastapi
uvicorn
yt-dlp

# ONNX Runtime - FASTEST for CPU (recommended for production)
onnxruntime>=1.16.0     # 2-3x faster than PyTorch on CPU

# Optional: For model export (not needed in production)
# super-gradients>=3.5.0  # Only needed to export ONNX models

# Detection Models (Apache 2.0 licensed) - fallback if no ONNX
transformers            # For RT-DETR
torch                   # PyTorch (only if not using ONNX)
torchvision

# super-gradients>=3.5.0  # YOLO-NAS - Deci AI (Apache 2.0) - Only if not using ONNX
supervision>=0.16.0      # ByteTrack tracking and utilities

# Optional: Ultralytics (AGPL-3.0 - DISABLED by default)
# Set ENABLE_ULTRALYTICS=true to use
# ultralytics  # Commented out - only install if testing required
